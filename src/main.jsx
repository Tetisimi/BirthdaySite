import React from "react";
import ReactDOM from "react-dom/client";
import AnitaBirthday from "../birthday";
import "./index.css";

ReactDOM.createRoot(document.getElementById("root")).render(
  <React.StrictMode>
    <AnitaBirthday />
  </React.StrictMode>
);
